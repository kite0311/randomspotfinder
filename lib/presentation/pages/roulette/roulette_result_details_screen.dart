//TODO: ルーレット結果の詳細画面
import 'package:flutter/material.dart';

class RouletteResultDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ルーレット結果の詳細'),
      ),
      body: Center(
        child: Text('ルーレット結果の詳細'),
      ),
    );
  }
}